name: Deploy Player to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - name: Use project npm version
        run: npm install --global npm@10.9.8

      - name: Install dependencies
        # npm ci currently fails on Linux while resolving this lockfile's
        # cross-platform optional native packages. npm install still honors
        # package-lock.json and installs the runner-specific binaries.
        run: npm install --no-audit --no-fund

      - name: Build Player
        env:
          REPOSITORY_NAME: ${{ github.event.repository.name }}
        run: npx ng build player --configuration production --base-href "/${REPOSITORY_NAME}/"

      - name: Prepare Pages artifact
        run: touch dist/player/browser/.nojekyll

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/player/browser

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
